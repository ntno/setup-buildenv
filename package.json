{
    "name": "setup-buildenv",
    "version": "0.0.0",
    "description": "installs Comcast/Buildenv-Tool",
    "main": "index.js",
    "scripts": {
        "lint": "eslint .",
        "prepare": "ncc build index.js -o dist --source-map --license license.txt",
        "test": "jest --passWithNoTests",
        "all": "npm run lint && npm run prepare && npm run test"
    },
    "keywords": [],
    "homepage": "https://github.com/ntno/setup-buildenv",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ntno/setup-buildenv.git"
    },
    "dependencies": {
        "@actions/core": "^1.2.6",
        "@actions/tool-cache": "^1.6.0"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.24.0",
        "eslint": "^7.4.0",
        "jest": "^26.1.0"
    }
}